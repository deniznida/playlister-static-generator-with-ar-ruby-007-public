<!DOCTYPE html>
<html>
<head>
<title><%= @genre.name %></title>
</head>
<body>
<h1>All Songs in <%= @genre.name %> Genre:</h1>
<ul>
<% @genre.songs.each do |song| %>
<li><a href="../songs/<%= song.to_slug %>.html"><%= song.name %></a></li>
<% end %>
</ul>
<h1>All Artists Included in <%= @genre.name %>:</h1>
<ul>
<% @genre.artists.each do |artist| %>
<li><a href="../artists/<%= artist.to_slug %>.html"><%= artist.name %></a></li>
<% end %>
</ul>
</body>
</html>